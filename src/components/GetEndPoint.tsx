

interface GetEndPointProps {
    path: string;
}

export default function GetEndPoint({ path }: GetEndPointProps) {
    return (
        <div>
            <h1>ここでAPI/weatherInfoAPI/root.tsを呼び出す</h1>
            <button className="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
                
                
            </button>
        </div>
    )
}